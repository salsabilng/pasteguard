# PasteGuard - Docker Compose (All-in-One Image)
#
# Production:
#   docker compose up -d
#
# Development (only Presidio, run Bun locally with hot-reload):
#   docker compose up presidio -d
#   bun run dev
#
# European languages:
#   PASTEGUARD_TAG=eu docker compose up -d
#
# Custom languages (local build):
#   LANGUAGES=en,de,ja docker compose up -d --build

services:
  # Production: Full all-in-one
  pasteguard:
    image: ghcr.io/sgasser/pasteguard:${PASTEGUARD_TAG:-en}
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        LANGUAGES: ${LANGUAGES:-en}
    ports:
      - "3000:3000"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./config.yaml:/pasteguard/config.yaml:ro
      - ./data:/pasteguard/data
    restart: unless-stopped

  # Presidio Analyzer Instance 1
#  presidio-1:
#    image: microsoft/presidio-analyzer:latest
#    environment:
#      - ANALYZER_CONF_FILE=/conf/analyzer.yaml
#    volumes:
#      - ./docker/presidio:/conf:ro
#    expose:
#      - "3000"
#    restart: unless-stopped

  # Presidio Analyzer Instance 2
#  presidio-2:
#    image: ghcr.io/microsoft/presidio-analyzer:latest
#    environment:
#      - ANALYZER_CONF_FILE=/conf/analyzer.yaml
#    volumes:
#      - ./docker/presidio:/conf:ro
#    expose:
#      - "3000"
#    restart: unless-stopped

